{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "streamlit run App/App.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false"
  }
}
